name: Simple Demo - Single Session

on:
  workflow_dispatch:

env:
  PROJECT_NAME: "Embrace Ecommerce"
  XCODE_VERSION: "16.4"

jobs:
  single-demo:
    runs-on: macos-latest
    timeout-minutes: 30

    steps:
    - name: Checkout code
      uses: actions/checkout@v5

    - name: Configure App with Repository Variables
      run: |
        echo "🔧 Configuring app with repository variables..."
        # Check if APP_ID variable exists
        if [[ -n "${{ vars.APP_ID }}" ]]; then
          echo "✅ APP_ID found in repository variables: ${{ vars.APP_ID }}"
          # Update the SDK configuration file
          echo "📝 Updating SDKConfiguration.swift..."
          sed -i '' 's/static let appId = "YOUR_EMBRACE_APP_ID"/static let appId = "${{ vars.APP_ID }}"/' "Embrace Ecommerce/Services/SDKConfiguration.swift"
          echo "✅ Updated SDK configuration"
          # Show the updated line for verification
          echo "📋 Updated line:"
          grep -n "static let appId" "Embrace Ecommerce/Services/SDKConfiguration.swift" || echo "⚠️ Could not find appId line"
        else
          echo "❌ ERROR: APP_ID variable not found in repository variables"
          echo "   Please add APP_ID as a repository variable before running this workflow."
          echo "   Go to Settings → Secrets and variables → Actions → Variables tab"
          exit 1
        fi
        echo ""

        # Set RUN_SOURCE to ci_action
        echo "📝 Configuring UITest to use ci_action as RUN_SOURCE..."
        sed -i '' 's/"RUN_SOURCE": "[^"]*"/"RUN_SOURCE": "ci_action"/' "Embrace EcommerceUITests/Embrace_EcommerceUITests.swift"
        echo "✅ RUN_SOURCE set to ci_action"
        echo ""

    - name: Set up Xcode
      run: |
        echo "🔧 Setting Xcode version to ${{ env.XCODE_VERSION }}..."
        sudo xcode-select -s /Applications/Xcode_${{ env.XCODE_VERSION }}.app
        xcodebuild -version

    - name: Find and Boot Simulator
      id: setup_simulator
      run: |
        echo "🔍 Finding iPhone simulator..."
        PROJECT_NAME="${{ env.PROJECT_NAME }}"
        SCHEME="${{ env.PROJECT_NAME }}"

        # Get valid destination from xcodebuild
        DESTINATION_LINE=$(xcodebuild -showdestinations -project "$PROJECT_NAME.xcodeproj" -scheme "$SCHEME" 2>&1 | grep "iPhone" | grep "platform:iOS Simulator" | head -1)

        if [[ -z "$DESTINATION_LINE" ]]; then
          echo "❌ No iPhone simulator found"
          exit 1
        fi

        # Extract the ID from the destination line
        SIMULATOR_UDID=$(echo "$DESTINATION_LINE" | grep -o 'id:[A-F0-9-]*' | cut -d: -f2)

        if [[ -z "$SIMULATOR_UDID" ]]; then
          echo "❌ Could not extract simulator ID"
          exit 1
        fi

        echo "✅ Found simulator with ID: $SIMULATOR_UDID"
        echo "simulator_udid=$SIMULATOR_UDID" >> $GITHUB_OUTPUT

        # Boot the simulator
        echo "⏳ Starting simulator..."
        xcrun simctl boot "$SIMULATOR_UDID" 2>/dev/null || echo "Already booted"
        xcrun simctl bootstatus "$SIMULATOR_UDID" -b > /dev/null 2>&1 || true
        echo "✅ Simulator is ready"

    - name: Build for Testing
      run: |
        echo "🔨 Building the app..."
        SIMULATOR_UDID="${{ steps.setup_simulator.outputs.simulator_udid }}"

        xcodebuild build-for-testing \
          -project "${{ env.PROJECT_NAME }}.xcodeproj" \
          -scheme "${{ env.PROJECT_NAME }}" \
          -destination "platform=iOS Simulator,id=$SIMULATOR_UDID" \
          -configuration Debug \
          -enableCodeCoverage NO \
          -allowProvisioningUpdates \
          -skipMacroValidation \
          CODE_SIGNING_ALLOWED=NO \
          CODE_SIGN_IDENTITY="" \
          CODE_SIGNING_REQUIRED=NO

        echo "✅ Build completed successfully"

    - name: Run UI Test with SDK Logging
      run: |
        echo "🧪 Running demo test with Embrace SDK logging..."
        SIMULATOR_UDID="${{ steps.setup_simulator.outputs.simulator_udid }}"
        PROJECT_NAME="${{ env.PROJECT_NAME }}"
        SCHEME="${{ env.PROJECT_NAME }}"
        TEST_CLASS="Embrace_EcommerceUITests"
        TEST_METHOD="testFlow"

        # Start capturing Embrace SDK logs in background (app + test runner)
        echo "📋 Starting Embrace SDK log capture..."
        log stream \
          --predicate 'processImagePath CONTAINS "Embrace" AND (process CONTAINS "Embrace Ecommerce" OR process CONTAINS "EcommerceUITests")' \
          --level debug \
          --style compact > embrace_sdk.log 2>&1 &

        LOG_PID=$!
        echo "Log capture PID: $LOG_PID"

        # Wait for log stream to initialize
        sleep 2

        # Run the test
        echo "▶️  Executing UI test..."
        xcodebuild test-without-building \
          -project "$PROJECT_NAME.xcodeproj" \
          -scheme "$SCHEME" \
          -destination "platform=iOS Simulator,id=$SIMULATOR_UDID" \
          -only-testing:"Embrace EcommerceUITests/$TEST_CLASS/$TEST_METHOD"

        echo "✅ Test completed successfully"

        # Wait for any remaining uploads (increased to 20s to allow termination uploads)
        echo "⏳ Waiting 20 seconds for SDK uploads to complete..."
        sleep 20

        # Stop log capture
        echo "🛑 Stopping log capture..."
        kill $LOG_PID 2>/dev/null || true
        sleep 1

    - name: Analyze Embrace SDK Logs
      if: always()
      run: |
        echo "═══════════════════════════════════════════════════════"
        echo "📊 EMBRACE SDK LOG ANALYSIS"
        echo "═══════════════════════════════════════════════════════"
        echo ""

        if [[ ! -f embrace_sdk.log ]]; then
          echo "❌ SDK log file not found!"
          exit 1
        fi

        echo "📋 Total log lines captured: $(wc -l < embrace_sdk.log)"
        echo ""

        # Check for SDK initialization
        echo "🔍 Checking SDK initialization..."
        if grep -q "Embrace.*start" embrace_sdk.log; then
          echo "✅ SDK initialized"
        else
          echo "⚠️  SDK initialization not detected"
        fi
        echo ""

        # Check for session lifecycle
        echo "🔍 Checking session lifecycle..."
        echo "Session events:"
        grep -i "session" embrace_sdk.log | head -10 || echo "  No session events found"
        echo ""

        # Check for upload attempts
        echo "🔍 Checking upload operations..."
        if grep -q "Upload operation" embrace_sdk.log; then
          echo "✅ Upload operations detected:"
          grep "Upload operation" embrace_sdk.log
        else
          echo "❌ No upload operations found in logs"
        fi
        echo ""

        # Check for successful uploads (HTTP 2xx status codes)
        echo "🔍 Checking for successful uploads..."
        if grep -E "Upload operation complete.*Status: 2[0-9]{2}" embrace_sdk.log; then
          echo "✅✅✅ SUCCESSFUL UPLOADS DETECTED! ✅✅✅"
          UPLOAD_COUNT=$(grep -cE "Upload operation complete.*Status: 2[0-9]{2}" embrace_sdk.log)
          echo "📈 Number of successful uploads: $UPLOAD_COUNT"
        else
          echo "❌ No successful uploads (Status 2xx) found"
        fi
        echo ""

        # Check for upload failures
        echo "🔍 Checking for upload failures..."
        if grep -E "Upload operation complete.*Status: [45][0-9]{2}" embrace_sdk.log; then
          echo "⚠️  Failed uploads detected:"
          grep -E "Upload operation complete.*Status: [45][0-9]{2}" embrace_sdk.log
        else
          echo "✅ No failed uploads"
        fi
        echo ""

        # Check for network errors
        echo "🔍 Checking for network errors..."
        if grep -i "error.*network\|network.*error\|connection.*failed" embrace_sdk.log; then
          echo "⚠️  Network errors detected:"
          grep -i "error.*network\|network.*error\|connection.*failed" embrace_sdk.log | head -5
        else
          echo "✅ No network errors detected"
        fi
        echo ""

        # Show upload URLs
        echo "🔍 Upload endpoints used:"
        grep -o "https://[^ ]*data.emb-api.com[^ ]*" embrace_sdk.log | sort -u || echo "  No URLs found"
        echo ""

        # Show full log for debugging (last 50 lines)
        echo "═══════════════════════════════════════════════════════"
        echo "📜 FULL SDK LOG (last 50 lines):"
        echo "═══════════════════════════════════════════════════════"
        tail -50 embrace_sdk.log
        echo ""
        echo "═══════════════════════════════════════════════════════"

    - name: Cleanup
      if: always()
      run: |
        echo "🧹 Cleaning up..."

        # Kill all simulator processes
        echo "🧹 Stopping all Simulator processes..."
        killall Simulator 2>/dev/null || true
        xcrun simctl shutdown all 2>/dev/null || true

        # Delete the simulator we created (if it still exists)
        SIMULATOR_UDID="${{ steps.setup_simulator.outputs.simulator_udid }}"
        if [[ -n "$SIMULATOR_UDID" ]]; then
          echo "🧹 Deleting simulator $SIMULATOR_UDID..."
          xcrun simctl delete "$SIMULATOR_UDID" 2>/dev/null || true
        fi

        # Clean up log files
        echo "🧹 Cleaning up log files..."
        rm -f embrace_sdk.log 2>/dev/null || true

        echo "✅ Cleanup complete"
